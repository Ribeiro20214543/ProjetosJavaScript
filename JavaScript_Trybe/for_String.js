/*No exercício de for com string, o objetivo é retornar a variável resultado como um array de strings, onde cada elemento desse array é o um caractere da variável frase , que é uma string.*/
for(var index = 0; index < frase.length; index++) {
  resultado.push(frase[index])
}